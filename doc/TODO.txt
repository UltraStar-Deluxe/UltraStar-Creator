notes from bohning:
===================
* undo last tap with rightclick on tap-button
* keyboard shortcut for tapping (V not working properly b/c of autorepeat, space only working because of manual focus to tap button)
* play mp3 only (to determine needed playback speed adjustment and default pitch for example)
* visualize current beat
* visualize note creation (progressbar?)
* replace b/# in default pitch by proper musical symbol
* add userdata to combobox_Language so that strings are translatable, but ultrastar file contains english language name
* fix resizing (especially size decrease)
* fix output to clipboard with special chars (encoding = UTF8)
* add proper shortcuts to different actions (open / save / ...
* add menus for inputlyrics (load from file / copy from clipboard / auto-clean / ...) and outputlyrics (save / copy to clipboard) and tags (option to turn id3-tag auto-fill off, set BPM manually, etc.)?
* work on documentation
* add action to input lyrics context menu with selection --> replace all occurences of this word by this syllable splitting
* get lyrics from internet
* undo when paused
* undo with step-back in mp3 time

notes from canni:
=================
* Lyrics: umlaute werden beim laden aus txt datei nicht angezeigt => utf-8
  => automatische encoding erkennung und #ENCODING tag einfügung?
  #ENCODING:CP1250, CP1252, UTF8, LOCALE, AUTO
	REMARK: true, true... (cannot reproduce for umlauts, but for other special characters, e.g. cyrillic letters)
* leere tags nicht mit in den output
	REMARK: empty tags are written on purpose to facilitate entering them at a later stage (empty tags don't bother anyone)
* hilfetexte überarbeiten (tw. kürzen, verständlicher oder keyboard shortcuts auflisten z.B. space für einen tap)
	REMARK: tooltips mostly removed and replaced by shorter statustips
* Und natürlich fehlende Features aus dem SongTapper vllt. übernehmen (Tonhöhen etc. blabla)
	REMARK: which features are missing? A default pitch is already available. Does SongTapper have a way to set pitches?
	
notes from thursday:
====================
* get lyrics from mp3
* add note that files are output to UTF8 (only compatible to USDX 1.1)
* usc_r79 fails to display properly on my Windows 7 netbook as previous versions had (see screenshots 1 & 2)
*  Clicking to load an mp3 and then cancelling causes the program to crash (see screenshot 3)
*  Attempting to load a cover without first loading an mp3 causes the program to crash
*  The ultrastar testing button is a great idea although I couldn't get it to work

notes from fepo:
================
1. MP3 / Artwork TAGS an erste Stelle setzen.
2. Habe bei einem Testsong das Problem, dass keine BPM angezeigt wird. BPM: 0 Nach mehrmaligem Laden ging es dann
3. Lässt sich ein ermittelter BPM Wert nicht nachträglich eintragen? ( Feld bleibt grau ). Wie ändere ich BPM manuell ?

notes from Hauke:
=================
* Wenn man mit der Maus in den Textbereich geht erscheint nach einiger Zeit die Hilfe als ein großer Kasten. Ich würde einen Hilfe-Button stark bevorzugen, weil ich viel mit der Maus arbeite und gerade, wenn ich am Bearbeiten bin, mir dieser Kasten die Sicht versperrt.
* Abstürze kann ich trotzdem noch konstruieren wenn...
	... ich auf mp3 aussuchen gehe und den Vorgang dann abbreche,
	... ich eine Video-Datei auswählen will, bevor ich eine mp3 ausgewählt habe,
	... ich eine Bild-Datei (CO/BG) auswähle bevor ich eine mp3 auswähle.
* Beim Auswählen von Video-Dateien hat das Programm noch Probleme. Bei mir hat das Programm meißt keine Video-Dateien angezeigt, obwohl welche vorhanden waren. Da wäre eine Auswahl "all file types" oder so hilfreich.

* mp3 abspielen, um Textversion mit mp3-Version zu vergleichen und Silbentrennung zu machen; * Download-/Suchfunktion für Cover, Background und (Video).

n